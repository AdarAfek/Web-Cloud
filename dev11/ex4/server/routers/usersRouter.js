
const { Router } = require("express");
const { vactionController } = require("../controllers/VactionsController");
const userRouter = new Router();

userRouter.get("/", );
userRouter.post("", ); 
userRouter.put("/", ); 
userRouter.delete("/",); 

module.exports = { vactionRouter };
